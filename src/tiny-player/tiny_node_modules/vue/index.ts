/**
 * Mock the bits of Vue that we need for the Tiny Player and omit the rest.
 */
export default {
    set( object: any, property: string, value: any ): void {
        object[ property ] = value;
    },
};
