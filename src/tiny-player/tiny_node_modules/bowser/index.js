/**
 * Mock the bits of Vue that we need for the Tiny Player and omit the rest.
 */
module.exports = {
    ios: false  // this query is used to determine whether hover is used, there is no interface, so the outcome doesn't matter.
};
